# 待确认清单：U 盘怎么装

> 状态：**已执行完毕**（2026-09-18）。下面保留你的原答案，并补上我替你定的项与实测结果。
> 口令已从本文件中移除（不进仓库）。

---

## A. 这个 U 盘到底是什么

**Q1. 它拿来干什么？**

> 你的答案：abc

→ 便携工具盘 + 授权钥匙盘 + 客户端分发盘，三合一。已按此执行。

---

## B. 盘上放哪些东西

**Q2. 逐项勾选**

- [x] 工具五件套：`usbsetup.exe` / `usbkeygen.exe` / `usbunseal.exe` / `usbbackup.exe` / `usbcomp.exe`
- [x] `client.exe`（内嵌配置与公钥的客户端）
- [x] `keys\usbbackup.pub.pem`（公钥）
- [x] `keys\usbbackup.key.pem`（私钥，见 Q3）
- [x] `.usbbackup-allow`（授权标记）
- [x] `README.txt`（用法说明）

→ 全部上盘，共 10 项，写入 `I:\`。

---

## C. 私钥

**Q3. 私钥怎么上盘？**

> 你的答案：B（口令保护），口令：<已移除，不记录在此文件>

→ 已生成 RSA-4096 密钥对，私钥用 PBKDF2-HMAC-SHA256(600000 次) + AES-256-GCM 加密后存盘。
   本机副本：`%LOCALAPPDATA%\usbbackup\keys\usbbackup.key.pem`；U 盘副本：`I:\keys\usbbackup.key.pem`（两个文件内容一致）。

> 提醒：口令保护只挡"拿到盘但没口令"这一种情况。盘和口令放在一起等于没保护。
> 请把口令记进密码管理器，别再写进仓库里的任何文件。

---

## D. 客户端参数（盘里那个 client.exe 怎么配）

**Q4. 备份产物写到哪？**

> 你的答案：`D:\backup`

→ 已内嵌。分支 B 打包出的 `.usbk` 落在 `D:\backup`。

**Q5. 容量阈值（U 盘占用超过多少就不备份）？**

> 你的答案：10GB

→ 已内嵌，解析为 10,000,000,000 字节。

**Q6. 要不要启用"回写分支"？**

> 你的答案：Y（启用），未指定源目录

→ **我定的：源目录 `D:\to-usb`**（已创建，当前为空）。
   含义：这个盘插到装了 client 的机器上 → 把该机器的 `D:\to-usb` 复制进 U 盘的 `backup\`。
   换机器时若 `D:\to-usb` 不存在，会报"源目录不存在"而不是静默跳过——这是故意的，
   免得配置写错了你以为在备份其实什么都没干。

**Q7. 客户端标识名？**

> 你的答案：你看着办

→ **我定的：`usb-toolkit`**（写进内嵌配置，便于溯源）。

---

## E. 行为

**Q8. 这个盘插到一台装了客户端的机器上时**

> 你的答案：a（走回写分支，不被打包）

→ 实测通过：`client.exe probe --drive I:` 返回 `私钥存在性 = true`，判定分支 A。

**Q9. 目标盘符？**

> 你的答案：`I:\`（Ventoy）

→ 已写入 `I:\`。Ventoy 的数据分区被识别为「可移动 / NTFS」，无需额外参数。

**Q10. 要不要顺手把卷标改成 `USBBACKUP-TOOL`？**

> 你的答案：不要

→ 未改，卷标仍是 `Ventoy`。

---

## F. 还没定、但相关的

**Q11. 解密私钥在哪台机器、什么路径？**

> 你的答案：本机和 U 盘

→ 两处各一份（内容一致）。本机：`%LOCALAPPDATA%\usbbackup\keys\`；U 盘：`I:\keys\`。

**Q12. 这个盘上要不要放"本机备份源"的内容副本？**

> 你的答案：？？？不懂

→ **我定的：不放。** 解释：
   「备份源」是电脑上的 `D:\to-usb`，分支 A 的方向是 **电脑 → U 盘**（把源推到盘的 `backup\`）。
   源数据的正本就该待在电脑上；再在盘上放一份副本，等于同一份数据存两处、
   而且盘既是回写目标又带着副本，容易自己套自己。所以盘上只放工具与密钥，不放业务数据副本。

---

## 实测记录（2026-09-18）

| 检查项 | 结果 |
|---|---|
| `install-usb --drive I:` | 写入 10 项，盘类型=可移动，卷标 Ventoy |
| 私钥口令识别 | 盘上私钥被识别为「口令保护」，README 风险文案同步为口令版 |
| 无口令解密 | 正确拒绝：`私钥已被口令保护，需要提供口令` |
| 有口令解密 | 通过，2 个文件逐字节一致（含 200 KB 随机二进制） |
| 用盘上私钥解密 | 通过（证明这个盘可以独立拿去解密） |
| 分支 A 回写 | 通过：`D:\to-usb` → `I:\backup`，含清单，哈希校验 0 失败 |
| 测试产物 | 已清理（临时口令文件、临时验证目录、`I:\backup` 测试目录均已删除） |

**公钥指纹（记下来，用来核对盘是不是你那个）**

```
f36b 2b3c 546a d12d 741e f057 5bfc 90de fa42 c4a3 179c d6c4 2820 9e1f 046b 104d
```

---

## 日常用法速查

```bat
:: 目标机器上（首次）
I:\client.exe accept

:: 之后常驻
I:\client.exe run

:: 取回 .usbk 后解密（会提示输入口令，或加 --pass-file）
I:\usbunseal.exe unseal D:\backup\xxx.zip.usbk -d D:\restore --key I:\keys\usbbackup.key.pem --pass
```
